{
  "name": "giphy-complete",
  "version": "1.0.0",
  "description": "I should find a more creative name. Maybe take the little ghost as the mascot.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently cd \"cd backend && npm run server\" \"cd frontend && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gabriska",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0"
  },
  "dependencies": {
    "styled-components": "^5.1.0"
  }
}
